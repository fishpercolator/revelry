{{!# 
Insert slide contents below, with one <section> for each slide. This
is a Handlebars template that uses your Revfile as context.
}}
<section>
  <h1>{{title}}</h1>
  <h2>{{description}}</h2>
</section>
